import UndefinedPath from '@/classes/paths/UndefinedPath.js';

describe('UndefinedPath class', () => {
	let undefinedPath;

	beforeEach(() => {
		undefinedPath = {
			$vd: {
				$getConfig: jest.fn(),
				$getMessage: jest.fn()
			},
			$getRules: jest.fn()
		};
	});

	it('', () => {
	});

});
